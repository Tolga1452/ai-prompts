/**
 * Tool: file_search.mclick
 * 
 * Description: Opens a given file from the search results.
 */
interface FileSearchMclickTool {
    // The exact input shape is not explicitly defined in the source, but is expected to open the clicked file and display its content.
}
