/**
 * Tool: user_info.get_user_info
 * 
 * Description: Get the user's current location and local time (or UTC time if location is unknown). You must call this with an empty json object {}
 */

interface UserInfoGetUserInfoTool {}
