/**
 * Tool: computer.switch_app
 * 
 * Description: Switches the computer's active application to `app_name`.
 */

interface ComputerSwitchAppInput {
    app_name: string; // App name. Only supported values are "chrome" and "libreoffice".
};
