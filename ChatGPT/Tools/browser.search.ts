/**
 * Tool: browser.search
 * 
 * Description: Searches for information related to `query`.
 */

interface BrowserSearchInput {
    query: string; // Search query.
    source?: string; // Browser backend.
};
