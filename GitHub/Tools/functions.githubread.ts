/**
 * Tool: functions.githubread
 * 
 * Description: Read and query GitHub data including repositories, issues, pull requests, files, file changes, alerts, discussions, and other GitHub resources.
 */

interface GithubReadTool {
    query: string; // The original user's question as a complete sentence for the query.
}
