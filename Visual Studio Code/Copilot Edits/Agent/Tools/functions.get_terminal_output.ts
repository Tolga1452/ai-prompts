/**
 * Tool: functions.get_terminal_output
 * 
 * Description: Get the output of a terminal command previous started with copilot_runInTerminal
 */

interface GetTerminalOutputInput {
    id: string; // The ID of the terminal command output to check.
};
